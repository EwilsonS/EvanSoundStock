const controller = require('./messagesController');
describe('#findAll', () => {
  it('should get all users from database', () => {
    //   let find = controller.findAll();
    //   expect(find).toBe(true)
    // });
    // it('should do something else', () => {
    //   expect(true).toBe(true)
  });
});